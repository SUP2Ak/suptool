export struct CleanGroup {
    name: string,
    enabled: bool,
    size: string,
    paths: [string],
}

export struct DetectedApp {
    name: string,
    paths: [string],
    size: string,
    cleanable: bool,
}