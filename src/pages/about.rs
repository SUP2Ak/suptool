use slint::Weak;
use slint::ComponentHandle;
use crate::slint_generated::MainWindow;


pub fn init(window: &Weak<MainWindow>) {
    // Initialisation de la page À propos
    // (généralement moins de logique ici car c'est une page statique)
}